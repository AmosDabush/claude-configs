# claude-configs

My Claude Code configuration files and custom commands.

## Structure
- `CLAUDE.md` - Global instructions
- `commands/` - Custom slash commands
- `agents/` - Agent configurations (future)

